# Required Secrets
OPENAI_API_KEY=

# Optional Overrides
PORT=3000
HOST=0.0.0.0
DATA_DIR=./backend/data
PDF_DIR=
WEBSITE_DIR=

# Frontend build config
# If unset, the frontend calls same-origin `/ask` (recommended).
# Set this only if your API is hosted on a different origin (e.g. https://api.example.com).
VITE_API_BASE_URL=
